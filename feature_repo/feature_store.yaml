project: currency_prediction
registry: data/registry.db
provider: local

# Для простоты используем файловое хранилище
online_store:
    type: sqlite
    path: data/online_store.db

offline_store:
    type: file